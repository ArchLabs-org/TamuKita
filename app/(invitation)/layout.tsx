/**
 * Invitation layout — intentionally minimal.
 * No marketing navbar or footer — invitation pages are full-screen.
 */
export default function InvitationLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
